# Plan semaine P2 - Mariem (Backend)

## Objectif

Renforcer la fiabilite backend pendant la finalisation frontend en parallele.

## Taches

1. Ecrire tests integration auth + patients.
2. Ecrire tests integration rendezvous + consultations.
3. Ecrire tests integration prescriptions + stock.
4. Ajouter endpoint `/api/readiness` avec etat services.
5. Finaliser documentation backend (setup, env, run, seed, tests, troubleshooting).

## Definition of done

- `npm run lint --workspace=backend` OK
- `npm run build --workspace=backend` OK
- `npm run test --workspace=backend` OK
