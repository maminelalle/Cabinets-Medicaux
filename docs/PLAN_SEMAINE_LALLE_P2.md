# Plan semaine P2 - Lalle (Frontend + Integration)

## Objectif

Terminer les modules UI restants et livrer un parcours utilisateur complet.

## Taches

1. Implementer page `medicaments` connectee API.
2. Implementer page `dossiers` connectee API.
3. Implementer page `rapports` (KPI + filtres).
4. Implementer pages `admin/users` et `settings`.
5. Faire une passe globale UX (loading, erreurs, etats vides, coherence).

## Definition of done

- `npm run lint --workspace=frontend` OK
- `npm run build --workspace=frontend` OK
- `npm run test --workspace=frontend` OK
